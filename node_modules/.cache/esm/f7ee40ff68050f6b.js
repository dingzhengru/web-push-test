let fs,webpush;_680‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_680‍.w("web-push",[["default",["webpush"],function(v){webpush=v}]]);


//* VAPID keys should only be generated only once.
const vapidKeys = webpush.generateVAPIDKeys();

_680‍.g.console.log('[PublicKey]', vapidKeys.publicKey);
_680‍.g.console.log('[PrivateKey]', vapidKeys.privateKey);

fs.writeFile('publicKey', vapidKeys.publicKey, err => {
  if (err) throw err;
  console.log('[PublicKey] 創建檔案成功');
});

fs.writeFile('privateKey', vapidKeys.privateKey, err => {
  if (err) throw err;
  console.log('[PrivateKey] 創建檔案成功');
});
